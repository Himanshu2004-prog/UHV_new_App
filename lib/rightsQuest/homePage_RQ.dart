import 'package:flutter/material.dart';
class homePage_RQ extends StatelessWidget {
  const homePage_RQ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Rights Quest"),),
      body: Center(child: Text("Rights Quest")),
    );
  }
}
