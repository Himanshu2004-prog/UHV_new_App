import 'package:flutter/material.dart';
class homePage_ER extends StatelessWidget {
  const homePage_ER({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Eco-Resolve"),),
      body: Center(child: Text("Eco-Resolve")),
    );
  }
}