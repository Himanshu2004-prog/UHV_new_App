import 'package:flutter/material.dart';
class homePage_mm extends StatelessWidget {
  const homePage_mm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Moral Mentor"),),
      body: Center(child: Text("Moral Mentor")),
    );
  }
}