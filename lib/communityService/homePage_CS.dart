import 'package:flutter/material.dart';

class homePage_CS extends StatelessWidget {
  const homePage_CS({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Community Service"),
      ),
      body: Center(child: Text("Community Service")),
    );
  }
}
