import 'package:flutter/material.dart';

class homePage_CR extends StatelessWidget {
  const homePage_CR({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Conflict Resolution"),
      ),
      body: Center(child: Text("Conflict Resolution")),
    );
  }
}
