import 'package:flutter/material.dart';
class homePage_PP extends StatelessWidget {
  const homePage_PP({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Prep Pal"),),
      body: Center(child: Text("Prep Pal")),
    );
  }
}